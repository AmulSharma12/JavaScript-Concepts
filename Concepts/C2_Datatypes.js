// 🎉Section 2- Data type in javascript

//the value here is the undefined so the variable is the undefined type
var undefinedVar = undefined
console.log(undefinedVar)

//the value here is the string so the variable is the string type 
var myName = "Anonymous"
console.log(myName)


//the value here is the number so the variable is the number type
var myAge = 21
console.log(myAge)

//the value here is the boolean value so the variable is the boolean type
//0 - false  1 - true
var isLegalToDrive = true
console.log(isLegalToDrive)

// 🎉For checking the type of variable in the javascript 
//typeof(variableName)
var isCheck = typeof(myName)
console.log(isCheck)

isCheck = typeof(myAge)
console.log(isCheck)

isCheck = typeof(undefinedVar)
console.log(isCheck)

isCheck = typeof(isLegalToDrive)
console.log(isCheck)